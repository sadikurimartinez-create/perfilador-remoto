import * as React from "react";

/**
 * CAPA 0 - BaseMapLayer
 * Responsable de la visualización del mapa base, calles, vialidades y referencias urbanas.
 * El estilo oscuro de alto contraste se aplica de manera nativa al mapa base.
 */
export const BaseMapLayer: React.FC = () => {
  return null; // El estilo oscuro está integrado directamente en las opciones de inicialización del mapa base
};

export default BaseMapLayer;
